<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Frontend App URL
    |--------------------------------------------------------------------------
    |
    | Base URL of the BasixMacro PWA (origin + app base path), used to build
    | links that point back into the frontend, e.g. password-reset emails.
    |
    */

    'url' => env('FRONTEND_APP_URL', 'https://localhost:3000/basixmacro'),

];
